pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm  // Checkout the code from the Git repository
            }
        }

        stage('Build') {
            steps {
                // Build the Docker image using docker-compose
                sh 'docker-compose -f "C:/Users/Eya/Desktop/Book Store/docker-compose.yml" build'
            }
        }

        stage('Test') {
            steps {
                // Ensure that run_tests.sh is executable
                sh 'chmod +x run_tests.sh'
                // Check the files in the workspace to verify if run_tests.sh exists
                sh 'ls -l'
                // Run the test script
                sh './run_tests.sh'
            }
        }

        stage('Deploy') {
            steps {
                // Deploy the app using docker-compose
                sh 'docker-compose -f "C:/Users/Eya/Desktop/Book Store/docker-compose.yml" up -d'
            }
        }
    }
}
